extends layout.pug

block content
  .ui.grid
    .four.wide.column
    .eight.wide.column
      form.ui.form.segment(method="post")
        .field.required
          label 用户名
          input(placeholder="用户名" type="text" name="name")
        .field.required
          label 密码
          input(placeholder="密码" type="password" name="password" autocomplete="false")
        input.ui.button.fluid(type="submit" value="登录")